<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Include Spring Boot's default configuration -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Console appender for development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File appender for production -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/synchboard.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/synchboard-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>10MB</maxFileSize>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Async appender for better performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- Error file appender -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/synchboard-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/synchboard-error-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>60</maxHistory>
            <maxFileSize>10MB</maxFileSize>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n%ex{full}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Spring profiles -->
    <springProfile name="dev,default">
        <!-- Development/Default logging configuration -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
        
        <!-- Application specific logging -->
        <logger name="io.github.sagimenahem.synchboard" level="DEBUG"/>
        
        <!-- Spring framework logging -->
        <logger name="org.springframework.web" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>
        <logger name="org.springframework.messaging" level="DEBUG"/>
        
        <!-- Database logging -->
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>
        
        <!-- ActiveMQ logging -->
        <logger name="org.apache.activemq" level="INFO"/>
    </springProfile>

    <springProfile name="prod">
        <!-- Production logging configuration -->
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
        
        <!-- Application specific logging -->
        <logger name="io.github.sagimenahem.synchboard" level="INFO"/>
        
        <!-- Spring framework logging -->
        <logger name="org.springframework" level="WARN"/>
        
        <!-- Security events should be logged in production -->
        <logger name="org.springframework.security" level="INFO"/>
        
        <!-- Database logging - minimal in production -->
        <logger name="org.hibernate" level="WARN"/>
        
        <!-- ActiveMQ logging -->
        <logger name="org.apache.activemq" level="WARN"/>
    </springProfile>

    <springProfile name="test">
        <!-- Test logging configuration -->
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
        </root>
        
        <!-- Application specific logging for tests -->
        <logger name="io.github.sagimenahem.synchboard" level="INFO"/>
        
        <!-- Reduce noise during tests -->
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
    </springProfile>

    <springProfile name="docker">
        <!-- Docker container logging configuration -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
        
        <!-- Application specific logging -->
        <logger name="io.github.sagimenahem.synchboard" level="INFO"/>
        
        <!-- Spring framework logging -->
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="org.springframework.security" level="INFO"/>
        <logger name="org.springframework.messaging" level="INFO"/>
        
        <!-- Database logging - moderate for Docker -->
        <logger name="org.hibernate.SQL" level="WARN"/>
        
        <!-- ActiveMQ logging -->
        <logger name="org.apache.activemq" level="INFO"/>
    </springProfile>
</configuration>
spring.application.name=backend

spring.datasource.url=jdbc:postgresql://localhost:5432/synchboard_db

spring.datasource.username=synchboard_user

spring.datasource.password=test12345

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update

application.security.jwt.secret-key=***REDACTED_DEV_JWT_KEY_V2***

# ActiveMQ Broker Configuration
spring.activemq.broker-url=tcp://localhost:61616
spring.activemq.user=admin
spring.activemq.password=admin

# STOMP Relay Configuration (for WebSocket)
spring.activemq.stomp.broker-host=localhost
spring.activemq.stomp.broker-port=61613

# Development profile - inherits from application.properties
# All main configuration should be in application.properties

# This file can contain development-specific overrides if needed
# Currently empty as all configuration is handled via environment variables